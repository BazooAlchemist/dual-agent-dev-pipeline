# Astra skill update

Scope: update the published dev-pipeline skill example to 2.0.0, matching the local installed skill. Document model-aware adoption while retaining the opt-in legacy dual-agent protocol, schemas and bridge. No deployment.

Verification: run the existing repository validator before and after the example update; compare the exported skill body to local installation; independent read-only review; git diff check and remote ref receipt.

No source code, schema, bridge or audit severity changes. Existing protocol remains applicable when explicitly adopted.
